<% dependsOn(["font-awesome", "p5"]); %>

<% title = "Top" %>

<link href='http://fonts.googleapis.com/css?family=Lora:400,700,400italic,700italic' rel='stylesheet'
      type='text/css'>
<link href='http://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,800italic,400,300,600,700,800'
      rel='stylesheet' type='text/css'>

<div class="ui right floated vertical compact menu">
    <a href="/demo" class="item">
        <i class="student icon"></i>
        Demos
    </a>
    <a href="/viz" class="item">
        <i class="bar chart icon"></i>
        Viz
    </a>
    <a href="/movie" class="item">
        <i class="video camera icon"></i>
        Movies
    </a>
    <a href="/music" class="item">
        <i class="music icon"></i>
        Musics
    </a>
    <a href="/project" class="item">
        <i class="lab icon"></i>
        Projects
    </a>
    <a href="/game" class="item">
        <i class="game icon"></i>
        Games
    </a>
    <a href="/tool" class="item">
        <i class="configure icon"></i>
        Tools
    </a>
    <a href="/watch" class="item">
        <i class="wait icon"></i>
        Watches
    </a>
    <a href="/helloworld" class="item">
        <i class="trash icon"></i>
        HelloWorld
    </a>
    <a href="/garbage" class="item">
        <i class="trash icon"></i>
        Garbages
    </a>
</div>

<style>
    #background{
        z-index: 0;
        position: absolute;
        width: 100%;
        height: 100%;
        left: -330px;
        top: -41px;
    }
    body{
        overflow-y: hidden;
    }
</style>

<script>
  function setup() {

    $("body").prepend("<div id='background'></div>");

    var width = $("#background").width();
    var height = $("#background").height() + 50;
    var canvas = createCanvas(width, height, WEBGL);
    canvas.parent("#background");
    ortho();

    // camera(0, -150, 1, 2, -150, 0, 0, 1, 0);
    camera(-180, 0, 200, 0, 0, 0, 0, 1, 0);

    fill(200);
    stroke(200);
  }

  var radius = 300;
  var speed = 0.005;

  // refs: https://qiita.com/hp0me/items/1570694d9f61368f3ae6
  // ortho(): https://processing.org/reference/ortho_.html

  function draw() {
    orbitControl();

    background(255);

    // var locY = (mouseY / height - 0.5) * (-2);
    // var locX = (mouseX / width - 0.5) * 2;
    // ambientLight(100, 80, 80);
    // pointLight(200, 200, 200, locX, locY, 0);
    // directionalLight(250, 250, 250, locX, -locY, 0.25);

    var _frameCount = frameCount;
    // rotateX(-0.6);
    rotateZ(0.5);

    for(var i = -9;i < 9;i++){
      push();
      rotateY(_frameCount * speed + TWO_PI * 2 * i / 18);
      torus(radius, 1, 48, 16);
      pop();
    }
    rotateX(HALF_PI);
    torus(radius, 1, 48, 16);

    for(var i = -8;i < 9;i++){
      push();
      translate(0,0,Math.sin((HALF_PI) * i / 9) * radius);
      torus(Math.cos((HALF_PI) * i / 9) * radius, 1, 48, 16);
      pop();
    }

    for(var i = -18;i < 18;i++){
      push();
      var ang = _frameCount * speed + TWO_PI * 2 * i / 36;
      translate(radius * 1.2 * sin(ang), radius * 1.2 * cos(ang), 0);
      rotateX(HALF_PI);
      cylinder(30, 1); // radius, height
      pop();
    }

  }

</script>